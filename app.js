const names={"סנהדרין":{id:"sanhedrin",title:"מסכת סנהדרין",weeks:75,end:"דף קי״ג ע״ב"},"בבא קמא":{id:"bavakama",title:"מסכת בבא קמא",weeks:79,end:"דף קי״ט ע״ב"}};const heads=["שבוע","תאריכי השבוע","פרשת השבוע","ימי לימוד","ההספק","הפרק"];function renderCards(){document.getElementById('cards').innerHTML=Object.entries(names).map(([k,n])=>`<article class="card" onclick="show('${n.id}')"><h2>${n.title}</h2><div class="meta"><span class="pill">${n.weeks} שבועות לימוד</span><span class="pill">סיום ${n.end}</span></div></article>`).join('')}function show(id){document.getElementById('home').style.display='none';document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));const key=Object.keys(names).find(k=>names[k].id===id),n=names[key],rows=DATA[key]||[];document.getElementById(id).innerHTML=`<div class="top"><button class="back" onclick="home()">← חזרה למסכתות</button><div class="title"><h2>${n.title}</h2><p>${n.weeks} שבועות לימוד · סיום ${n.end}</p></div><input id="q-${id}" placeholder="חיפוש לפי שבוע, פרשה, דף או פרק…" oninput="filter('${id}')"></div><div class="tablebox"><table><thead><tr>${heads.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody id="body-${id}">${rows.map(r=>`<tr>${r.map(v=>`<td>${v??'—'}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;document.getElementById(id).classList.add('active');window.scrollTo({top:0,behavior:'smooth'})}function filter(id){const q=document.getElementById('q-'+id).value.trim().toLowerCase();document.querySelectorAll('#body-'+id+' tr').forEach(tr=>tr.style.display=tr.innerText.toLowerCase().includes(q)?'':'none')}function home(){document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));document.getElementById('home').style.display='block';window.scrollTo({top:0,behavior:'smooth'})}window.addEventListener('kinyan-data-ready',renderCards);